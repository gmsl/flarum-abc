import { Store } from 'mem-fs';
import { Paths } from 'boilersmith/paths';
export declare function npmPath(name: string): string;
export declare function composerPath(name: string): string;
export declare function extensionPath(name: string): string;
export declare function corePath(_name: string): string;
declare type PackageInfo = {
    name: string;
    gitRemote: string;
    /**
     * Defaults to `main`.
     */
    mainBranch?: string;
};
export declare type FlarumMonorepoJsonSchema = {
    packages: {
        core?: PackageInfo;
        extensions: PackageInfo[];
        composer?: PackageInfo[];
        npm?: PackageInfo[];
    };
};
export declare function getMonorepoConf(fs: Store, paths: Paths): FlarumMonorepoJsonSchema;
export {};
