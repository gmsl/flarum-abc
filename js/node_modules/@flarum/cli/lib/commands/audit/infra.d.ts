import { StepManager } from 'boilersmith/step-manager';
import BaseCommand from '../../base-command';
import { FlarumProviders } from '../../providers';
export default class AuditInfra extends BaseCommand {
    static description: string;
    static flags: {
        fix: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
        monorepo: import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
    };
    static args: {
        name: string;
        description: string;
    }[];
    protected steps(stepManager: StepManager<FlarumProviders>, extRoot: string): StepManager<FlarumProviders>;
    protected additionalPreRunChecks(extRoot: string): Promise<void>;
}
