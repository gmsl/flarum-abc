import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { BaseJsStep } from './base';
export declare class GenerateModelDefinition extends BaseJsStep {
    type: string;
    protected getDefinition(io: IO): Promise<string>;
    protected getImports(frontend: string, paths: Paths, io: IO): Promise<string>;
    exposes: never[];
    getExposed(_paths: Paths, _paramProvider: IO): Record<string, unknown>;
}
