import { Store } from 'mem-fs';
import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { Validator } from '../../../utils/validation';
import { BasePhpStubStep } from '../php-base';
export declare class GenerateMigrationStub extends BasePhpStubStep {
    type: string;
    protected schema: {
        root: string;
        recommendedSubdir: string;
        forceRecommendedSubdir: boolean;
        sourceFile: string;
        params: {
            name: string;
            type: string;
            message: string;
            validate: typeof Validator.migrationName;
        }[];
    };
    protected compileParams(fs: Store, paths: Paths, io: IO): Promise<Record<string, unknown>>;
    protected getFileName(fs: Store, paths: Paths, _paramProvider: IO): Promise<string>;
}
