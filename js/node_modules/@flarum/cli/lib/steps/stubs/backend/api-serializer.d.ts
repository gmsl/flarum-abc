import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { Validator } from '../../../utils/validation';
import { BasePhpStubStep } from '../php-base';
import { Store } from 'mem-fs';
export declare class GenerateApiSerializerStub extends BasePhpStubStep {
    type: string;
    protected additionalExposes: string[];
    protected additionalImplicitParams: string[];
    protected phpClassParams: string[];
    protected schema: {
        recommendedSubdir: string;
        sourceFile: string;
        params: ({
            name: string;
            type: string;
            message: string;
            validate: typeof Validator.className;
        } | {
            name: string;
            type: string;
            message: string;
            validate?: undefined;
        })[];
    };
    protected compileParams(fs: Store, paths: Paths, io: IO): Promise<Record<string, unknown>>;
}
