import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { Validator } from '../../../utils/validation';
import { BasePhpStubStep } from '../php-base';
import { Store } from 'mem-fs';
export declare class GenerateModelStub extends BasePhpStubStep {
    type: string;
    protected additionalExposes: string[];
    protected phpClassParams: never[];
    protected schema: {
        recommendedSubdir: string;
        sourceFile: string;
        params: ({
            name: string;
            type: string;
            message: string;
            validate: typeof Validator.className;
            optional?: undefined;
        } | {
            name: string;
            type: string;
            message: string;
            validate?: undefined;
            optional?: undefined;
        } | {
            name: string;
            type: string;
            message: string;
            validate: typeof Validator.tableName;
            optional: boolean;
        })[];
    };
    protected compileParams(fs: Store, paths: Paths, io: IO): Promise<Record<string, unknown>>;
}
