import { Store } from 'mem-fs';
import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { FlarumBaseStubStep } from './flarum-base';
export declare abstract class BasePhpStubStep extends FlarumBaseStubStep {
    protected defaultRoot: string;
    get exposes(): string[];
    get implicitParams(): string[];
    protected phpClassParams: string[];
    protected precompileParams(fs: Store, paths: Paths, io: IO): Promise<Record<string, unknown>>;
    protected getFileName(_fs: Store, _paths: Paths, io: IO): Promise<string>;
    protected stubNamespace(packageNamespace: string, paths: Paths): string;
}
