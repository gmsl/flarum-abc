import { Store } from 'mem-fs';
import { IO } from 'boilersmith/io';
import { Paths } from 'boilersmith/paths';
import { DefaultProviders, Step } from 'boilersmith/step-manager';
export declare class UpdateJSImports implements Step {
    type: string;
    composable: boolean;
    run(fs: Store, paths: Paths, _paramProvider: IO, _providers: DefaultProviders): Promise<Store>;
    exposes: never[];
    getExposed(_paths: Paths, _paramProvider: IO): Record<string, unknown>;
}
