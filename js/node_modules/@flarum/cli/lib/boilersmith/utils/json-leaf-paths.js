"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.jsonLeafPaths = void 0;
function inner(o, prefix) {
    if (Array.isArray(o)) {
        // Placed separately in case we eventually decide to support leaves in arrays.
        return [prefix.join('.')];
    }
    if (o && typeof o === 'object') {
        return Object.entries(o).flatMap(([k, v]) => inner(v, [...prefix, k]));
    }
    return [prefix.join('.')];
}
function jsonLeafPaths(obj) {
    return inner(obj, []);
}
exports.jsonLeafPaths = jsonLeafPaths;
